# 更新点

> 按日期汇总 TCMP 功能更新，面向使用者。技术细节与**每次代码变更的完整记录**见 [CHANGELOG.md](CHANGELOG.md)（开发/部署必写）。
>
> **部署状态**
> - **2026-09-11** 用例集移动 / 模块树拖动：**已部署 151 + 127**（`deploy/windows/deploy_dist_patch.py`，2026-09-11）。
> - **2026-09-04** 模块树双击重命名：**已部署 151 + 127**。
> - **2026-09-03** 本节功能：**已部署 151 + 127**（`deploy/windows/deploy_dist_patch.py`，2026-09-03）。
> - **127 后端 Watchdog**：**已部署 127**。
> - **2026-08-11 及更早**：已部署 151 + 127。

---

## 2026-09-11 · 用例集移动到其他项目

### 用例集项目详情页
- 用例集列表每行新增「**移动**」；勾选多个后工具栏可点「**移动到…**」。
- 在弹窗中选择目标**用例集项目**即可迁移；用例、模块树、评审记录**不变**，仅变更所属项目。
- 已在目标项目中的用例集会自动跳过（批量移动时提示）。

### 模块树拖动
- **用例集详情**、**用例执行**页左侧树：可**拖动**子模块 / 子功能 / 测试项名称调整位置。
- 同级拖动为排序；拖入上一级节点内可变更所属模块（用例仍挂在原模块节点上，路径会同步更新）。
- 顶层「模块」与用例行不可拖动。

---

## 2026-09-04 · 模块树双击重命名

### 用例集详情 · 左侧模块树
- **子模块、子功能、测试项**（树中 level 2～4 节点）：**双击名称**即可行内修改。
- Enter 或点击空白处保存；Esc 取消；名称不能为空，最长 64 字；同级不能有重名。
- 顶层「模块」节点不支持在此重命名（仍可通过用例编辑调整路径）。

---

## 2026-09-03 · 工作台排序、默认首页与用例集列表优化

### 一、用例集列表（用例集项目详情页）

#### 1. 支持按名称模糊查询
- 工具栏右侧新增搜索框，输入关键字即可筛选用例集，**不区分大小写**（输入 `2d` 也能搜到「2D标定」），中文关键字同样可用。
- 边输入边查询，清空搜索框恢复完整列表；无匹配时会提示「没有名称包含 xxx 的用例集」。

#### 2. 隐藏「ID」列
- 列表不再显示 ID 列，页面更聚焦于名称和描述。

#### 3. 按更新时间排序
- 新增「更新时间」列，取该用例集下**用例最近一次修改的时间**（用例集里还没有用例时，显示用例集自身的更新时间）。
- 列表默认按更新时间**从新到旧**排列，最近在维护的用例集排在最前；点击「更新时间」表头可切换升序/降序。

#### 4. 删除用例集加权限
- 删除用例集是**不可恢复**的操作（会连同用例、版本记录、模块树、评审记录一起删掉），因此收紧了权限：**只有该用例集的创建者或系统管理员**才能删除。
- 无权限时「删除」按钮置灰，鼠标悬停会提示原因。
- 注意：如果某个用例集是别人创建的，你将无法删除它，需要找创建者或系统管理员处理。
- 本次只调整了「删除整个用例集」；用例集内部删除单条用例、批量删除、清空用例集等操作的权限**未变**。

### 二、项目

#### 1. 子项目 / 模块功能测试项目支持删除
- 在项目详情页的「子级项目」Tab 中，卡片视图和列表视图都新增了「删除」按钮（内置项目除外）。
- **删除子项目**：需先删除其下全部「模块功能测试项目」。
- **删除模块功能测试项目**：需先删除或关闭其下全部测试轮次；项目用例池中的引用会一并清除。
- 删除前会弹出确认框，并提示上述约束；若条件不满足，后端会返回具体原因。

#### 2. 测试轮次：双击重命名
- 模块功能测试项目详情页的「测试轮次」列表中，**双击轮次名称**即可行内编辑重命名。
- 按 Enter 或点击其他地方保存；按 Esc 取消；名称不能为空，最长 64 字。

### 三、用例评审

#### 1. 导出评审意见
- 评审详情页右上角新增「**导出评审意见**」按钮，可下载 Excel 文件。
- 导出内容包含：
  - **评审概要**：标题、状态、用例集、成员、用例数、意见数等；
  - **用例与意见**：每条评审意见一行，并附带对应用例的编号、名称、等级、模块、步骤、预期结果等完整信息；
  - **整体意见**：不针对单条用例的总体建议。
- 仅评审发起人和评审成员可导出（与查看评审详情权限一致）。

### 四、工作台

#### 1. 待执行用例按时间排序
- 工作台「待执行用例」列表改为**按测试轮次创建时间倒序**排列，最新建的轮次排在最前面（原来按待执行数量从多到少排）。
- 列表新增「创建时间」列；点击「创建时间」或「待执行」表头可自行切换升序/降序。
- 「待办评审」列表同样新增「创建时间」列，可点表头排序。

#### 2. 进入平台默认落到工作台
- 登录成功后进入「工作台」（原来进入「项目」页）。
- 直接访问平台首页（如 `http://192.168.20.127/`）也会自动进入工作台。
- 说明：别人分享的具体页面链接（如某个项目、某条评审）仍可直达，不会被跳转。

---

## 2026-07-16

### 一、新功能

#### 1. 子项目支持「版本迭代」配置
- 新建子项目时，原来的「Teambition URL」输入框改为「版本迭代」输入框（如 `3.0.1`、`迭代S1`，可留空）。
- 顶层项目的新建/设置里仍保留「Teambition URL」，未受影响。

#### 2. 导入到项目用例池：可就地新建目标项目
- 「导入到项目用例池」对话框里，目标项目下拉框旁新增「新建」按钮：选择一个上级子项目、填写名称，即可当场创建「模块功能测试项目」并自动选中，不用先跳去项目页创建。

#### 3. 导入完成后一键前往项目详情
- 导入成功后，对话框显示成功提示，并提供「前往项目详情」按钮，直接跳转到该项目页面查看导入结果。

#### 4. 导入用例自动去重
- 向项目用例池导入用例时，会自动过滤重复用例：
  - 同一条用例（相同用例）已在池中 → 跳过；
  - 内容完全相同（标题、等级、前置条件、测试步骤、测试数据、预期结果都一样）的用例 → 跳过（包括本次导入批次内部的重复）。
- 导入后提示「已导入 X 条，跳过 Y 条重复」。

#### 5. 删除用例时同步清理空的模块节点
- 删除用例后，如果其所在的子模块 / 子功能 / 测试项节点下已经没有用例、也没有子节点，会自动删除这些空节点，左侧模块树同步更新。
- 说明：用例删除仍为「软删除」（标记删除、数据保留，可恢复），并非物理删除。

#### 6. 用例编辑：选中行上/下插入 + 保存自动按顺序重排编号
- 在用例批量编辑弹窗里，点击行首「#」序号列可选中该行（高亮）。
- 工具栏新增「↑ 上方插入行」「↓ 下方插入行」，在选中行的上方或下方插入空白行，并自动带上该行的子模块 / 子功能 / 测试项 / 等级，方便在同一测试项下补充用例。
- 保存时，用例编号会按当前行的顺序自动重新生成（从 0001 开始、前缀不变，如 `M85028_0001`、`M85028_0002`…），插入的行也会获得顺序编号，重新打开后顺序保持一致。

#### 7. 修复：XMind 导入时预期结果被误判为测试步骤
- 现象：当预期结果以 "step" 开头（如 `step运行正常`）时，导入转 Excel 后预期结果变成空白，并多出一条把预期当作步骤的行。
- 修复：优化步骤识别规则，`步骤/step` 后必须带数字或分隔符才算步骤；纯 `step运行正常` 会正确作为预期结果导入。

#### 8. XMind 导入：内容去重 + 测试项合并用「-」连接
- 内容去重：整棵 XMind 里内容完全相同的用例只入库一条（防止源脑图把子树复制多份导致重复导入），导入结果会显示跳过的重复数。
- 测试项分隔符：当测试项由多个子节点合并而成时，子节点之间用 `-` 连接（原来是 `/`），如 `pose测试-单pose`。

#### 9. 用例集「全部删除」（筛选范围）一次删完 + 编辑保存更稳
- 选中左侧模块节点或搜索后点「全部删除」，改为一次批量软删（原来逐条请求，量大时慢且易卡）。
- 用例批量编辑：修复部分行保存失败重试时可能重复创建的问题；单条编辑保存失败现在会明确提示。

### 二、体验与性能优化

#### 10. 用例集详情页支持大数据量（3000+ 用例）
- 主表改为分页显示（默认每页 50 条，可切换 50 / 100 / 200 / 500），进入详情、切换左侧模块节点明显更快。
- 「导入到项目用例池」「全部删除」仍作用于当前筛选的全部用例，不只当前页。

#### 11. 用例批量编辑弹窗支持大数据量（1000+ 用例）
- 弹窗改为「增量渲染」：打开时先显示前 80 行，向下滚动到底部再自动加载下一批，弹窗秒开、不再卡顿。
- 底部显示「已显示 X / 总数」提示；新增行、粘贴数据后新行立即可见。

#### 12. 用例批量编辑：单元格文字完整换行显示
- 子模块 / 子功能 / 测试项 / 用例名称 / 前置条件 / 测试步骤 / 测试数据 / 预期结果 / 标签等列，内容自动换行完整显示，不再被截断。

### 三、问题修复

#### 13. 修复：批量编辑弹窗在大节点打开时页面卡死
- 优化文字自动增高的计算方式，避免上万个单元格同时触发大量重排导致页面无响应。

#### 14. 修复：批量编辑弹窗空白单元格无法输入
- 当某行内容较多把整行撑高时，同行的空白单元格原来只有一小块可点击区域，点其它位置无法输入。
- 修复后，同一行所有单元格高度一致、铺满整格，点击单元格任意位置都能输入。

---

## 2026-08-11 · 用例集详情展示优化

### 一、体验优化

#### 1. 用例列表增加「用例名称」列
- 用例集详情页右侧主表，在「测试项」与「等级」之间新增「用例名称」列，便于快速识别每条用例。

#### 2. 左侧模块树隐藏空节点
- 子功能、测试项下没有任何用例时，左侧树不再显示对应节点，减少无效点击。
- 模块、子模块节点仍正常显示（便于在该层级新增用例）。

---

### 一、新功能

#### 1. 用例集列表页：勾选多个用例集「发起评审」
- 在子项目下的用例集列表页，可勾选多个用例集，点击「发起评审」一次性创建跨多个用例集的评审单。
- 评审范围覆盖所选全部用例集内的活跃用例；评审详情页会显示涉及的用例集名称。

#### 2. 评审进行中仍可调整评审成员
- 发起者在评审状态为「评审中」时，可点击「管理评审成员」增删评审人。
- 被移除的评审人若曾标记「评审完成」，其完成状态会同步清除，避免状态不一致。

#### 3. 评审进行中仍可添加 / 移除用例
- 发起者在「评审中」可点击「添加用例」，从本评审覆盖的用例集中勾选尚未纳入的用例加入评审。
- 支持从当前评审范围内移除某条用例（同时删除该用例在本评审下的全部意见）。

#### 4. 整体意见 / 建议
- 评审详情页新增「整体意见 / 建议」区域，评审成员与发起人可发表不针对单条用例的总体看法。
- 用例列表「意见」列改为展示具体评论摘要（作者 + 内容），不再只显示条数。

#### 5. 评审中即可修订用例（不必等到「修订中」）
- 发起者在「评审中」和「修订中」两个阶段都可以直接修订用例内容（分栏 / 平铺视图均支持）。
- 多用例集评审修订时，会按用例所属用例集正确加载模块树。

### 二、体验与性能优化

#### 6. 评审详情：大数据量更流畅
- 分栏视图下用例列表增加分页（默认每页 100 条），避免一次渲染过多行卡顿。
- 评论展示改为按用例索引缓存，减少列表滚动时的重复计算。

### 三、安全与稳定性

#### 7. 评审读接口权限收紧
- 查看评审详情、评论列表、可添加用例候选列表时，需为评审发起人或评审成员；非参与者无法通过接口读取。
- 「可添加用例」列表仅发起人可见。

#### 8. 批量与长度上限
- 单次跨用例集评审最多 20 个用例集、最多 1 万条用例；单次添加用例最多 2000 条；单条评论最多 1 万字符；评审标题最多 200 字。

### 四、涉及的主要代码（供开发对照）

| 模块 | 文件 |
|------|------|
| 后端实体 | `backend/src/entities/case-review.entity.ts`（新增 `caseSetIds`） |
| 后端服务 | `backend/src/modules/reviews/reviews.service.ts` |
| 后端接口 | `backend/src/modules/reviews/reviews.controller.ts` |
| 前端 API | `frontend/src/api/index.ts` |
| 评审详情 | `frontend/src/views/CaseReviews/ReviewDetail.vue` |
| 多用例集发起 | `frontend/src/views/CaseReviews/StartMultiReviewDialog.vue` |
| 用例集列表 | `frontend/src/views/CaseSets/GroupDetail.vue` |
| 部署脚本 | `deploy/windows/deploy_dist_patch.py`、`deploy/windows/_ssh_util.py` |

### 五、数据存储说明

- **多用例集范围**：`case_reviews.caseSetIds`（JSON 数组）
- **当前评审用例列表**：`case_reviews.caseIdsSnapshot`（追加 / 移除后更新）
- **评审成员**：`case_reviews.reviewerUserIds`、`completedReviewerIds`
- **整体意见**：`case_review_comments` 表中 `caseId = 0` 的记录
- **单条用例意见**：同表 `caseId > 0`
- 暂无单独的「操作流水」表；刷新后看到的是当前最新快照。
