# 用例管理和执行系统 产品需求规格书（PRD）

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 用例管理和执行系统（Test Case Management & Execution Platform，下文简称 TCMP / 本系统） |
| 文档版本 | v1.0（初稿） |
| 状态 | Draft – 待评审 |
| 读者 | 研发团队（前端、后端、测试、运维） |
| 编写日期 | 2026-06-04 |
| 源需求 | `用例管理和执行系统需求.docx`（共 11 个功能点） |

> 说明：本 PRD 基于源需求文档中"功能 1 ～ 功能 11"逐条扩展，对原文未明确细节给出**推荐方案**并在第 12 章"附录 A 开放问题"中显式列出，需求评审时确认。所有 API、字段、状态、错误码以本 PRD 为准。

---

## 修订记录

| 版本 | 日期 | 作者 | 描述 |
| --- | --- | --- | --- |
| v1.0 | 2026-06-04 | PM | 初稿，覆盖源需求 11 个功能点 |

---

## 目录

1. 产品概述
2. 用户角色与权限矩阵
3. 业务流程与状态机
4. 功能需求详细规格
5. 数据模型
6. 接口与集成需求
7. UI / 页面结构与交互
8. 非功能需求
9. 技术约束与依赖
10. 验收标准
11. 里程碑与版本规划
12. 附录（开放问题、术语表、风险）

---

## 1. 产品概述

### 1.1 背景与问题

公司当前测试用例分散于 Excel / Word / 个人电脑，缺乏统一管理；测试执行结果靠人工汇总，缺陷与用例无法溯源，多轮回归无法横向对比。需要一个 Web 化、可在线编辑、可分配执行、可联动缺陷系统的平台。

### 1.2 目标

- **统一存储**：以"用例集"为载体集中维护用例资产，支持导入与在线编辑。
- **按项目复用**：项目按需从用例集挑选用例，支持多轮回归。
- **执行闭环**：测试人员在线执行、修订用例，失败直接联动缺陷系统（TB）建单。
- **可视化**：自动生成轮次报告与项目级缺陷看板。
- **协作**：通过钉钉推送任务，降低沟通成本。

### 1.3 范围

| 在范围内 | 不在范围内（V1） |
| --- | --- |
| 用例集管理（CRUD、导入、在线编辑） | 自动化用例脚本执行 |
| 项目管理 + 轮次执行 | 性能测试管理 |
| 钉钉任务推送 | 测试环境管理 |
| 缺陷系统（TB）联动建单 | 测试需求管理（RM） |
| 轮次报告与缺陷看板 | 测试资源/工时统计 |
| 邮箱注册、登录、RBAC | SSO（V1 可选，见 §6.4） |

### 1.4 价值指标（V1 上线 3 个月内）

| KPI | 目标 |
| --- | --- |
| 用例资产数字化率 | ≥ 90% |
| 单轮次执行汇总耗时 | 从 4h → < 10min |
| 缺陷与用例关联率 | ≥ 95% |

---

## 2. 用户角色与权限矩阵

### 2.1 角色定义

| 角色 | 描述 | 默认归属 |
| --- | --- | --- |
| 系统管理员（SysAdmin） | 平台级配置、用户管理、TB/钉钉集成配置 | 平台 |
| 项目管理员（ProjectAdmin / PM） | 项目内最高权限：成员、轮次、用例库映射 | 项目级 |
| 测试负责人（TestLead） | 创建轮次、制定策略、分配用例、生成报告 | 项目级 |
| 测试执行人（Tester） | 执行被分配的用例、提交缺陷、修订用例 | 项目级 |
| 用例库维护员（CaseLibOwner） | 维护用例集（新增/导入/编辑/归档） | 用例集级 |
| 只读访客（Viewer） | 查看用例、报告、看板 | 项目级 |

> 角色可叠加（同一用户在不同项目持不同角色）。系统级权限独立于项目级权限。

### 2.2 权限矩阵（功能 × 角色）

`✔ = 允许；✘ = 禁止；▲ = 仅自己创建或被分配的对象`

| 功能 | SysAdmin | PM | TestLead | Tester | CaseLibOwner | Viewer |
| --- | :---: | :---: | :---: | :---: | :---: | :---: |
| 用户注册 / 登录 / 改密 | ✔ | ✔ | ✔ | ✔ | ✔ | ✔ |
| 用户启停 / 角色分配（系统级） | ✔ | ✘ | ✘ | ✘ | ✘ | ✘ |
| 配置 TB / 钉钉 / 邮件 | ✔ | ✘ | ✘ | ✘ | ✘ | ✘ |
| 创建用例集 | ✔ | ✔ | ✘ | ✘ | ✔ | ✘ |
| 导入 / 在线编辑用例 | ✔ | ✔ | ✘ | ▲ | ✔ | ✘ |
| 删除 / 归档用例集 | ✔ | ✘ | ✘ | ✘ | ✔ | ✘ |
| 创建项目 | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |
| 项目成员管理 | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |
| 从用例集挑选用例入项目 | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ |
| 创建测试轮次 | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ |
| 制定筛选策略 / 分配用例 | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ |
| 执行用例（F/P/Block/NP/NT） | ✔ | ✔ | ✔ | ▲ | ✘ | ✘ |
| 执行界面修订 / 删除 / 新增用例 | ✔ | ✔ | ✔ | ▲ | ✔ | ✘ |
| 提交缺陷（联动 TB） | ✔ | ✔ | ✔ | ▲ | ✘ | ✘ |
| 生成 / 查看轮次报告 | ✔ | ✔ | ✔ | ✔ | ✘ | ✔ |
| 查看缺陷看板 | ✔ | ✔ | ✔ | ✔ | ✘ | ✔ |
| 钉钉推送配置 | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |

---

## 3. 业务流程与状态机

### 3.1 端到端业务流程

```
[CaseLibOwner] 创建用例集 → 导入/在线录入用例
            │
            ▼
[PM] 创建项目 → 按筛选条件从用例集挑选用例入项目
            │
            ▼
[TestLead] 创建测试轮次 → 配置筛选策略（含上一轮结果）→ 分配给 Tester
            │
            ▼ 钉钉推送
[Tester] 执行用例（F/P/Block/NP/NT）
   ├─ F  → 弹出"提交缺陷" → 自动回填步骤+版本 → 在 TB 创建缺陷 → 关联用例
   ├─ 修订/新增/删除用例 → 同步回用例集（版本化）
            │
            ▼
[TestLead] 轮次结束 → 生成轮次报告（执行率/缺陷数/模块执行率/缺陷列表）
            │
            ▼
[全员] 查看项目缺陷看板
```

### 3.2 用例（TestCase）状态机

```
DRAFT ──提交──► REVIEW ──通过──► ACTIVE ──废弃──► ARCHIVED
  ▲                │ 驳回             │
  └────────────────┘                  └──修订──► ACTIVE（版本号 +1）
```

| 状态 | 含义 | 可执行 | 入轮次 |
| --- | --- | --- | --- |
| DRAFT | 草稿（在线新建） | ✘ | ✘ |
| REVIEW | 待评审（可选流程，见开放问题 OP-3） | ✘ | ✘ |
| ACTIVE | 已生效 | ✔ | ✔ |
| ARCHIVED | 已归档（保留历史） | ✘ | ✘ |

### 3.3 用例执行结果状态机（功能 7）

| 结果 | 全称 | 含义 | 是否可提缺陷 | 是否计入"执行率" |
| --- | --- | --- | :---: | :---: |
| `P` | Pass | 通过 | ✘ | ✔ |
| `F` | Fail | 失败 | ✔（强制） | ✔ |
| `BLOCK` | Block | 阻塞，前置条件不满足无法执行 | ✔（可选） | ✔ |
| `NP` | Not Performed | 主动不执行（如不涉及） | ✘ | ✘（按推荐方案，见 OP-4） |
| `NT` | Not Tested | 当前版本功能未实现，无法执行 | ✘ | ✘ |
| `PENDING` | 待执行（默认初始态） | – | ✘ | ✘ |

> 执行率公式：`执行率 = (P + F + BLOCK) / (总用例数 - NT - NP) × 100%`（OP-4 待确认）

### 3.4 测试轮次状态机

```
DRAFT ──发布──► IN_PROGRESS ──结束──► CLOSED
                    │
                    └──暂停──► PAUSED ──恢复──► IN_PROGRESS
```

| 状态 | 可分配 | 可执行 | 可生成报告 |
| --- | :---: | :---: | :---: |
| DRAFT | ✘ | ✘ | ✘ |
| IN_PROGRESS | ✔ | ✔ | ✔（实时） |
| PAUSED | ✘ | ✘ | ✔（实时） |
| CLOSED | ✘ | ✘ | ✔（终版快照） |

---

## 4. 功能需求详细规格

> 命名约定：FR-X 对应源需求"功能 X"。每个 FR 包含：功能描述、前置条件、主流程、异常流、业务规则、字段校验、权限。

### FR-1 用户注册（源：功能 1）

**功能描述**：使用公司邮箱注册，自定义密码。

**前置条件**：邮箱域名属于公司白名单（`@<company>.com`，可配置，多域名支持）。

**主流程**
1. 用户在登录页点击"注册"，填写：邮箱、密码、确认密码、姓名、手机号（可选）。
2. 后端校验邮箱域名 + 唯一性 + 密码强度。
3. 发送邮箱验证码（6 位，10 分钟内有效）。
4. 用户输入验证码，注册成功，账号默认角色为 `Viewer`，状态 `PENDING_ASSIGN`，需管理员或 PM 邀请加入项目。

**异常流**
- 邮箱域名不在白名单：返回 `E1001 邮箱域名不允许`。
- 邮箱已存在：`E1002 邮箱已注册`。
- 密码不符合强度：`E1003 密码强度不足`。
- 验证码错误或过期：`E1004 验证码无效`。

**业务规则**
- 密码强度：≥ 8 位，至少包含数字 + 字母 + 特殊字符。
- 同一邮箱 24h 内最多发送 5 次验证码。
- 注册成功后自动登录。

**字段校验**

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| email | string | RFC 5322；domain ∈ 白名单；唯一 |
| password | string | 8–64；强度同上；加密存储（bcrypt cost=12） |
| name | string | 1–32 字符 |
| phone | string | 可空；中国大陆手机号正则 |

**权限**：公开（无需登录）。

---

### FR-2 登录（源：功能 2）

**功能描述**：邮箱 + 密码登录，发放访问令牌。

**主流程**
1. 邮箱 + 密码 → 后端校验 → 颁发 JWT（access token 2h + refresh token 7d）。
2. 记录登录日志（IP、UA、时间）。

**业务规则**
- 连续 5 次密码错误，账号锁定 15 分钟（可解锁）。
- 支持"记住我"（refresh token 延长至 30d）。
- 登出：服务端使 refresh token 失效（黑名单 / Redis）。

**权限**：公开。

> 扩展：V1.5 支持 LDAP / 钉钉扫码登录，见 §6.4。

---

### FR-3 用例集管理（源：功能 3）

**功能描述**：用例的资产库。支持新建、命名、导入、在线编辑、删除、归档。

**主流程**
1. CaseLibOwner 进入"用例集管理"页 → "新建用例集" → 输入名称、所属业务线（可选）、描述。
2. 进入用例集详情页，可：
   - **导入**：上传 Excel/XMind/CSV（模板由系统提供下载）。
   - **在线新建**：填写用例字段（见 §5.2 TestCase 实体）。
   - **批量编辑**：勾选用例 → 改优先级 / 标签 / 模块归属。
   - **导出**：Excel / CSV。
3. 用例集可设置 `可见范围`：私有 / 指定项目 / 全员可见。

**用例字段（与 `用例模版.xlsx` 完全对齐）**

模块级元数据（在用例集"模块"节点上维护，导入时取自 Excel 表头前 4 行）：

| 字段 | 必填 | 说明 |
| --- | :---: | --- |
| 模块描述 | ✘ | 富文本 |
| 测试环境 | ✘ | 富文本 |
| 测试日期 | ✘ | date；模块基线日期 |
| 测试人员 | ✘ | 模块负责人/参与人，多值 |

用例字段：

| 字段 | 必填 | 类型 / 取值 | 说明 |
| --- | :---: | --- | --- |
| 用例编号 | ✔ | varchar(64) | 在线新增由系统按 `<模块Code>_<4位序号>` 生成（如 `XJGL_0001`）；导入时若为空也按规则补齐；用例集内唯一 |
| 模块（Sheet 名） | ✔ | 字符串 | 顶层模块；导入时一个 Sheet = 一个顶层模块 |
| 子模块 | ✔ | 字符串 | 二级 |
| 子功能 | ✔ | 字符串 | 三级 |
| 测试项 | ✔ | 字符串 | 四级 |
| 用例名称 | ✔ | varchar(200) | 标题 |
| 用例等级 | ✔ | enum | `P0` / `P1` / `P2` / `P3` |
| 前置条件 | ✘ | 富文本 | |
| 测试步骤 | ✔ | 富文本 / 有序列表 | 支持 `1. … 2. …` 多步 |
| 测试数据 | ✘ | 富文本 | 与"步骤"对齐的输入数据 |
| 预期结果 | ✔ | 富文本 / 有序列表 | 与步骤一一对应 |
| 执行方式 | ✘ | enum | `手工` / `自动化` / `半自动`（默认 `手工`） |
| 用例类型 | ✘ | enum | `业务` / `功能` / `接口` / `兼容性` / `性能` / `安全` / `易用性` / `其他` |
| 标签 1 ～ 标签 5 | ✘ | 字符串 × 5 | 自由文本；用于轮次筛选 |
| 测试阶段 | ✘ | enum | `冒烟` / `系统` / `回归` / `验收` / `上线前` |
| 执行结果 | – | enum | 仅在轮次执行实例上有值；用例本体留空 |
| 创建人 / 修改人 / 时间 | 系统 | – | |
| 版本号 | 系统 | int | 修订时自增 |

> 模块层级：**模块 / 子模块 / 子功能 / 测试项**（4 级）。导入时由 Sheet 名 + 三列动态构建模块树；同一路径已存在则复用。

**导入规则**
- 模板字段固定；表头校验失败整文件拒绝并返回错误行号。
- 重复（按"用例编号"或"用例标题 + 模块"）→ 用户选择：跳过 / 覆盖 / 新增副本。
- 单次导入 ≤ 5000 条；超出按 5000/批分批上传。

**异常流**
- 文件 > 20MB：`E3001 文件过大`。
- 格式不符：`E3002 表头不匹配`。
- 字段超长：返回行号与字段名。

**权限**：CaseLibOwner / PM / SysAdmin（按 §2.2）。

> **开放问题 OP-1**：源文档"用例的格式如下"附图未提供，本 PRD 字段为推荐方案，待评审。

---

### FR-4 项目管理（源：功能 4）

**功能描述**：项目是测试活动的载体。从用例集挑选用例入项目，建立"项目用例池"。

**主流程**
1. PM "新建项目"：名称、Key（唯一）、描述、关联产品线、关联 TB 项目（见 FR-9）、成员。
2. 进入项目 → "用例池" → "从用例集添加用例"。
3. **筛选挑选**：
   - 来源用例集：多选。
   - 模块：树形多选。
   - 优先级：多选（P0–P3）。
   - 标签：多值；条件可 AND / OR 组合。
   - 用例类型、创建人、创建时间区间。
4. 预览匹配结果 → 勾选 → "加入项目"。

**业务规则**
- 项目用例池内的用例是**用例集用例的引用快照**（保留 `caseSetCaseId + version`）；用例集后续修订不自动同步进项目（V1 推荐）；可手动"刷新版本"。详见 OP-2。
- 同一用例可被多个项目引用；引用计数维护。
- 项目删除：软删除（30 天可恢复），关联轮次同步隐藏。

**字段校验**

| 字段 | 约束 |
| --- | --- |
| projectKey | 大写字母 + 数字，3–10 位，全局唯一 |
| name | 1–64 字符，项目内唯一 |

**权限**：PM / SysAdmin 创建；项目成员可见。

---

### FR-5 测试轮次（源：功能 5）

**功能描述**：项目下按"轮次"组织一次完整执行（如冒烟、回归、上线前 P0 轮）。

**主流程**
1. TestLead "新建轮次"：轮次名（如"V2.3 回归 R1"）、被测软件版本号、计划开始/结束时间、关联需求版本（可选）。
2. **筛选策略配置**（必选其一为基础，可叠加多个条件）：
   - **全部用例**：项目用例池中全部。
   - **按用例等级**：勾选 P0/P1/P2/P3。
   - **按上一轮执行结果**：仅 F / 仅 Block / 仅 NT / 仅 F+Block；要求选择"上一轮次"作为参照。
   - **按标签值**：多值 + AND/OR。
3. 条件组合语法：每条件为一节点，节点间用 AND/OR 拼接；UI 提供"添加条件组"支持嵌套（深度 ≤ 3）。后端以**布尔表达式树**存储（见 §5.3）。
4. 预览匹配结果 → 确认 → 进入"分配"步骤。
5. **分配**：按用例勾选 → 指派给项目成员（一人或多人，多人=多份执行实例）；支持按"模块均分""按数量均分"快速分配。
6. 发布轮次 → 触发钉钉推送（FR-6）。

**业务规则**
- 同一项目同一时刻可有多个 IN_PROGRESS 轮次。
- 轮次发布后可"追加用例"或"重新分配"，但已有执行记录不丢失。
- 一份用例可被分配多次（"多人执行同一用例"），系统记录为多个 `RoundCaseInstance`，每份独立结果。
- 轮次关闭后不可执行，仅可查看。

**异常流**
- 上一轮参照不存在或未结束：`E5001 上一轮次无可用结果`。
- 筛选结果为 0：`E5002 无匹配用例`（允许保存为草稿）。

**权限**：TestLead / PM。

---

### FR-6 钉钉推送（源：功能 6）

**功能描述**：用例分配 / 重新分配 / 轮次发布时，通过钉钉推送给相关人员。

**主流程**
1. 轮次发布或追加分配后，后端按"被分配人 → 钉钉 userId"映射，调用钉钉工作通知 API（OA / 工作消息）。
2. 推送内容：项目名、轮次名、被分配用例数、截止时间、跳转链接（深链至执行页）。
3. 用户钉钉点击 → 跳转到本系统的执行页（需登录态，未登录引导）。

**业务规则**
- 推送策略：
  - 单次分配 ≤ 50 条 → 即时推送。
  - 单次分配 > 50 条 → 聚合一条消息。
  - 同一人 5 分钟内多次分配 → 合并推送。
- 失败重试：3 次指数退避；终态失败入"推送失败队列"，管理员可手动重试。
- 用户可在"个人设置"关闭钉钉推送（仅影响个人）。

**配置项（SysAdmin）**
- 钉钉企业 AppKey / AppSecret / AgentId。
- 邮箱 → 钉钉 userId 映射（按手机号 / 邮箱自动匹配 + 手动覆盖）。

**接口**：见 §6.3。

---

### FR-7 用例执行（源：功能 7）

**功能描述**：执行轮次中分配给自己的用例。

**页面结构**

```
┌─────────────────────────────────────────────────────────────┐
│ 顶栏：项目 > 轮次 名 / 版本号 / 进度 / 退出                       │
├──────────────┬──────────────────────────────────────────────┤
│ 左侧（30%）   │ 右侧（70%）                                    │
│ 树状图        │  用例详情 + 执行区                              │
│ ├ 模块 A      │  ─ 标题 / 编号 / 优先级 / 版本                  │
│ │ ├ 用例 1 ●  │  ─ 前置条件                                    │
│ │ └ 用例 2    │  ─ 步骤表（step / data / expected / actual）    │
│ └ 模块 B      │  ─ 实际结果（富文本，必填当 F/Block）           │
│ 过滤：状态/分配│  ─ 执行结果按钮：[P] [F] [Block] [NP] [NT]    │
│              │  ─ 提交缺陷（仅 F 强制弹窗；Block 可选）         │
│              │  ─ 备注 / 附件（截图）                          │
└──────────────┴──────────────────────────────────────────────┘
```

**树状图节点状态指示**
- 颜色：绿 P / 红 F / 灰 PENDING / 黄 Block / 蓝 NP / 紫 NT
- 节点显示模块汇总：`6/10` 已执行/总数

**主流程**
1. 左树选用例 → 右侧加载详情。
2. 选择执行结果 → 必填项校验：
   - F：必填"实际结果"+ 强制弹"提交缺陷"对话框（FR-9）。
   - Block：必填"实际结果"。
   - NP / NT：必填"说明"。
3. 保存 → 自动跳转下一条 PENDING 用例（可配置开关）。

**业务规则**
- 用例的"分配人"和"执行人"可不同（Tester 可代他人执行，但需角色 ≥ TestLead）。
- 执行结果可修改，每次修改记录历史（操作日志）。
- 附件：单文件 ≤ 10MB，支持 png/jpg/log/txt/zip。

**快捷键**
- `↑/↓` 切换用例；`P/F/B/N/T` 快捷打结果；`Ctrl+S` 保存。

**异常流**
- 网络中断：本地暂存（IndexedDB），恢复后提示同步。
- 并发：同一执行实例被多端打开 → 后保存覆盖；前端实时提示"他人正在执行"。

**权限**：Tester（仅自己被分配的）/ TestLead / PM。

---

### FR-8 执行界面修订用例（源：功能 8）

**功能描述**：执行过程中发现用例步骤/预期有误，可直接修订；支持新增、删除用例。修订/新增/删除**同步回源用例集**。

**主流程**
1. 在执行详情页点击"编辑用例" → 进入编辑态（与用例集编辑表单一致）。
2. 保存时弹确认："此修改将同步到用例集 `<name>` 并生成新版本 vN+1，确认？"
3. 后端原子操作：
   - 更新用例集 → 新版本号。
   - 当前项目用例池引用版本号自动升级。
   - 当前轮次已分配实例的"用例快照"**保留旧版本**（保证执行历史一致性），但 UI 显示"用例已更新，点击查看新版本"。
4. 新增/删除：
   - 新增：在用例集追加，并自动加入当前轮次。
   - 删除：用例集软删除；引用方处理见 OP-2。

**业务规则**
- 修订需校验当前用户对源用例集有写权限；无权限 → 走"提议修改"流程（生成 PR-like 单，由 CaseLibOwner 审批）。OP-3
- 删除 P0 用例需 PM 二次确认。
- 操作日志：记录变更前后 diff。

**权限**：Tester（仅自己分配的用例 + 有源用例集写权限）/ CaseLibOwner / PM。

---

### FR-9 缺陷提交联动（源：功能 9）

**功能描述**：用例结果为 F 时，提供"提交缺陷"入口，与 **TB（Teambition，External）** 联动建单，自动回填测试步骤、被测版本，并关联用例。

**TB 平台说明（已最终确定）**
- 平台：阿里 **Teambition**（`https://www.teambition.com`）。
- 项目缺陷分组示例 URL：`https://www.teambition.com/project/60b75d1971a638c8771a9ab6/bug/section/635bbbc175417100120310a0`
  - `projectId` = `60b75d1971a638c8771a9ab6`
  - `bugSectionId`（缺陷分组 ID）= `635bbbc175417100120310a0`
- 项目设置中需保存：`tb_project_id`、`tb_bug_section_id`（默认入哪个分组），均可手填或从 URL 自动解析。

**前置条件**
- 项目已配置 `tb_project_id` + `tb_bug_section_id`（PM 在项目设置中粘贴 Teambition 缺陷分组 URL，系统自动解析）。
- 系统已配置 Teambition OAuth 应用凭证（SysAdmin）。

**主流程**
1. F 触发"提交缺陷"对话框（也可在用例详情 → 缺陷 Tab 主动触发）。
2. 表单字段（自动回填部分以 `[auto]` 标注）：

| 字段 | 来源 |
| --- | --- |
| 缺陷标题 | 用户填写（默认建议："[模块] 用例标题 - 异常") |
| 严重程度 | 用户选（Blocker/Critical/Major/Minor/Trivial） |
| 优先级 | 用户选（P0–P3） |
| 复现步骤 | `[auto]` 用例步骤格式化文本 + 实际结果 |
| 预期结果 | `[auto]` 来自用例 |
| 实际结果 | `[auto]` 来自执行 |
| 被测软件版本 | `[auto]` 来自轮次 |
| 关联用例 | `[auto]` 当前用例编号 |
| 关联轮次 | `[auto]` 当前轮次 |
| TB 项目 / 缺陷分组 | `[auto]` 项目配置（`tb_project_id` + `tb_bug_section_id`） |
| 经办人 | 用户选（下拉来自 Teambition 项目成员） |
| 附件 | 上传（与执行附件同源） |

3. 用户确认 → 后端调用 TB Create Issue API → 返回 TB issueKey → 本系统记录 `DefectLink(roundCaseInstanceId, tbIssueKey, tbProjectKey, createdAt)`。
4. 用例详情页显示该缺陷链接（跳转到 TB）。

**业务规则**
- 单个 F 实例可提交多个缺陷。
- 同步策略：
  - **创建**：实时同步到 TB。
  - **状态回写**：定时（每 5min）从 TB 拉取关联缺陷状态（New/In Progress/Resolved/Closed/Rejected），更新本系统缓存。
  - **删除**：本系统不删除 TB 缺陷，仅解除关联。
- TB 调用失败：本系统保存"草稿缺陷"，提示"TB 暂不可达，已暂存，稍后将自动重试"。

**异常流 / 错误码**

| 码 | 含义 | 处理 |
| --- | --- | --- |
| E9001 | TB 项目未关联 | 引导 PM 去项目设置 |
| E9002 | TB API 401 / 403 | 告警 SysAdmin |
| E9003 | TB 5xx / 超时 | 入重试队列 |

**权限**：Tester（自己执行的 F 实例）/ TestLead / PM。

> **开放问题 OP-5**：TB 平台具体形态？（自研 / Jira / Bugzilla / 禅道）API 协议需在评审时确认；本 PRD 以"RESTful + Bearer Token"为假设。

---

### FR-10 轮次报告（源：功能 10）

**功能描述**：轮次结束（或进行中实时）生成测试报告，统计执行率、缺陷数、模块执行率、缺陷列表。

**报告结构（与 `轮次测试报告.xlsx` 完全对齐，导出 Excel 复用此模板）**

报告分为 2 个 Sheet：

#### Sheet 1：总体说明

（1）**测试总体说明**（基本信息表）

| 字段 | 来源 | 备注 |
| --- | --- | --- |
| 项目名称 | 自动（项目） | |
| 本轮测试策略 | 自动（轮次 filterExpr 文本化）+ 可手填补充 | |
| 软件版本路径 | 手填（轮次设置可保存） | 安装包/构建产物路径 |
| 测试版本 | 自动（轮次 softwareVersion） | |
| 测试时间 | 自动（actual_start ~ actual_end） | |
| 测试环境 | 手填 | 富文本 |
| 测试人员 / 测试任务 | 自动（按分配维度聚合） | 每人对应被分配模块/用例数 |

（2）**测试结果总结**：手填富文本。

（3）**测试充分性说明**（按模块两列）

| 列 | 含义 | 公式 |
| --- | --- | --- |
| 各功能本轮测试充分性 | 当前轮次该模块执行用例数 / 模块总用例数 × 100% | `executed_in_this_round / total_in_module` |
| 功能截止本轮总体测试充分性 | 截止本轮（**含本轮**）累计执行用例数 / 模块总用例数 × 100% | `cumulative_executed_through_and_including_this_round / total_in_module` |

> 口径已最终确定：**包含本轮**（即累计 = 本轮之前所有轮次执行数 + 本轮执行数）。同一用例在多个轮次被执行时按 distinct 用例计数（一个用例仅计一次），实现上以 `COUNT(DISTINCT case_id) WHERE result IN ('P','F','BLOCK')` 计算。

模块颜色规则（取色与 `轮次测试报告.xlsx` 对齐；Excel 表头沿用模板浅灰）：

| 区间 | 颜色 | 十六进制 / Excel ARGB | 含义 |
| --- | --- | --- | --- |
| ≥ 90% | 绿色 | `#92D050` / `FF92D050` | 全面测试 |
| 30% ~ 90% | 黄色 | `#FFFF00` / `FFFFFF00` | 部分测试 |
| < 30% | 红色 | `#FF0000` / `FFFF0000` | 基本未测试 |
| 表头底色 | 浅灰 | `#D9D9D9` / `FFD9D9D9` | 与模板一致 |

> 说明：黄、红、灰三色直接取自源模板 `轮次测试报告.xlsx`；绿色在模板中未出现，按同套调色板补齐为 Excel 标准绿 `#92D050`，可在评审时调整。

（4）**附件说明**：手填，可上传文件附件清单（默认 `无`）。

#### Sheet 2：缺陷汇总

| 列 | 字段 | 来源 |
| --- | --- | --- |
| NO.# | 序号 | 自动 |
| 缺陷标题 | TB issue summary | 自动 |
| 缺陷描述 | TB issue description | 自动 |
| 缺陷严重级 | TB severity | 自动 |
| 缺陷状态 | TB status（最新同步值） | 自动 |
| 出现概率 | `必现` / `高概率` / `低概率` / `偶现` | 提缺陷时录入 |
| 提交人 | 用户名 | 自动 |
| 备注 | 富文本 | 手填 |

#### 报告页在线视图（在 Excel 之外的增强展示）

| 区块 | 内容 |
| --- | --- |
| 执行汇总卡片 | 总用例数 / Pass / Fail / Block / NP / NT / 执行率 / 通过率 |
| 人员维度表 | 每执行人：分配数、已执行、通过率、平均耗时 |
| 缺陷图表 | 按严重程度饼图、按状态柱图、按模块 Top10 |
| 风险与结论 | 富文本（同步进 Sheet1 "测试结果总结"） |

**指标定义**

| 指标 | 公式 |
| --- | --- |
| 执行率 | (P + F + Block) / (Total - NT - NP) × 100% |
| 通过率 | P / (P + F + Block) × 100% |
| 本轮充分性（按模块） | 本轮该模块执行数 / 模块总用例数 × 100% |
| 截止本轮充分性（按模块） | 累计执行数 / 模块总用例数 × 100% |
| 缺陷密度 | 缺陷数 / 已执行用例数 |

**功能**
- 在线查看（HTML）+ 导出 **Excel（套用 `轮次测试报告.xlsx` 模板，含颜色标记）** / PDF。
- 轮次关闭时自动生成"快照版本"（保证数据不变）。
- 进行中可生成"实时版本"。
- 历史报告可比对（V1.5 可选）。

**权限**：项目成员可看；SysAdmin 全局可看。

> **开放问题 OP-6**：源文档"模版参照这个"附图未提供，需求评审时提供模板以微调报告版式。

---

### FR-11 项目缺陷管理与看板（源：功能 11）

**功能描述**：每个项目维度的缺陷集中视图与看板（汇总从 FR-9 提交的 + 周期性从 TB 同步的）。

**列表视图**

| 列 | 说明 |
| --- | --- |
| TB Key | 链接到 TB |
| 标题 / 严重 / 优先级 / 状态 | 同步自 TB |
| 关联用例 | 跳本系统用例 |
| 关联轮次 | 跳本系统轮次 |
| 提交人 / 经办人 / 创建时间 | – |

筛选：状态、严重、提交人、轮次、模块、时间区间、关键字。

**看板（Dashboard）卡片**
- 缺陷总数 / 未关闭数 / 本周新增 / 本周关闭。
- 按严重程度分布（饼图）。
- 按状态分布（柱图）。
- 按模块 Top10（柱图）。
- 趋势：近 30 天新增 vs 关闭折线图。
- 老化（Open 天数分布）：0–3 / 4–7 / 8–14 / 15+ 天。
- 修复 SLA 达标率（按严重 SLA 阈值，默认值见 §8.3，可项目级配置）。

**导出**：Excel / PDF。

**权限**：项目成员均可看；SysAdmin 全局可看。

---

## 5. 数据模型

### 5.1 ER 概览

```
User ──*──< UserProjectRole >──*── Project
                                    │
                                    ├──< Round
                                    │       ├──< RoundCaseInstance >── (execution result)
                                    │       │           │
                                    │       │           └──< DefectLink ──► TB Issue (ext)
                                    │       └── filterExpr (JSON)
                                    │
                                    └──< ProjectCaseRef ──► CaseSetCase
                                                              │
                                                              └──< CaseVersion
            CaseSet ──< CaseSetCase
                          │
                          └── Tags / Module（树）
```

### 5.2 实体字段定义

#### 5.2.1 User

| 字段 | 类型 | 约束 | 备注 |
| --- | --- | --- | --- |
| id | bigint PK | – | 雪花 ID |
| email | varchar(128) | UNIQUE NOT NULL | 公司邮箱 |
| password_hash | varchar(128) | NOT NULL | bcrypt |
| name | varchar(64) | NOT NULL | |
| phone | varchar(20) | NULL | |
| dingtalk_user_id | varchar(64) | NULL | 钉钉映射 |
| status | enum | ACTIVE/LOCKED/DISABLED/PENDING_VERIFY | |
| created_at / updated_at | datetime | | |

索引：`email`, `dingtalk_user_id`。

#### 5.2.2 Role / UserProjectRole / UserSystemRole

- `Role(code, name)`：枚举见 §2.1。
- `UserSystemRole(user_id, role_code)`：仅 `SysAdmin`。
- `UserProjectRole(user_id, project_id, role_code)`：联合唯一。

#### 5.2.3 CaseSet

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| code | varchar(32) | UNIQUE，用例编号前缀 |
| name | varchar(128) | UNIQUE |
| description | text | NULL |
| visibility | enum | PRIVATE / PROJECT / PUBLIC |
| owner_user_id | bigint FK User | |
| status | enum | ACTIVE / ARCHIVED |
| created_at / updated_at | datetime | |

#### 5.2.4 CaseSetCase（用例本体，与 `用例模版.xlsx` 对齐）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| case_set_id | bigint FK | NOT NULL |
| code | varchar(64) | UNIQUE；格式 `<moduleCode>_<seq:0000>`（如 `XJGL_0001`） |
| title | varchar(200) | NOT NULL（对应"用例名称"） |
| module_id | bigint FK Module | NOT NULL；指向 4 级模块叶节点 |
| priority | enum | `P0` / `P1` / `P2` / `P3`（对应"用例等级"） |
| type | enum | `BUSINESS` / `FUNCTION` / `API` / `COMPAT` / `PERF` / `SECURITY` / `UX` / `OTHER` |
| execution_mode | enum | `MANUAL` / `AUTO` / `SEMI_AUTO`（默认 MANUAL） |
| test_stage | enum | `SMOKE` / `SYSTEM` / `REGRESSION` / `ACCEPTANCE` / `PRE_RELEASE` |
| precondition | text | 富文本 |
| steps | text | 富文本（保留原始换行编号） |
| test_data | text | 富文本 |
| expected_result | text | 富文本 |
| tag_1 ～ tag_5 | varchar(64) × 5 | 同时建立 `tags JSONB` 镜像列用于复合查询 |
| current_version | int | NOT NULL，默认 1 |
| status | enum | DRAFT/REVIEW/ACTIVE/ARCHIVED |
| created_by / updated_by | bigint FK User | |
| created_at / updated_at | datetime | |

索引：`(case_set_id, module_id)`, `priority`, `test_stage`, GIN(`tags`)（PostgreSQL）。

#### 5.2.5 CaseVersion（用例历史版本）

| 字段 | 类型 | 备注 |
| --- | --- | --- |
| id | bigint PK | |
| case_id | bigint FK | |
| version | int | |
| snapshot | json | 整条用例快照 |
| change_note | varchar(500) | |
| changed_by | bigint FK | |
| changed_at | datetime | |

唯一：`(case_id, version)`。

#### 5.2.6 Module（模块树，固定 4 级：模块/子模块/子功能/测试项）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| case_set_id | bigint FK | |
| parent_id | bigint FK Module | NULL（顶层为 NULL） |
| level | tinyint | 1=模块 / 2=子模块 / 3=子功能 / 4=测试项 |
| name | varchar(64) | |
| code | varchar(32) | 顶层模块业务编码（如 `XJGL`），用作用例编号前缀 |
| path | varchar(500) | 物化路径 `/模块/子模块/子功能/测试项` |
| order_no | int | 同级排序 |
| description | text | 模块描述（仅顶层维护） |
| test_environment | text | 模块默认测试环境（仅顶层维护） |
| baseline_date | date | 模块基线日期（仅顶层维护） |
| owners | json | string[]，模块测试人员（仅顶层维护） |

#### 5.2.7 Project

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| project_key | varchar(10) | UNIQUE，大写字母+数字 |
| name | varchar(64) | UNIQUE |
| description | text | |
| tb_project_id | varchar(32) | NULL；Teambition projectId（例 `60b75d1971a638c8771a9ab6`） |
| tb_bug_section_id | varchar(32) | NULL；Teambition 缺陷分组 ID（例 `635bbbc175417100120310a0`） |
| tb_bug_section_url | varchar(500) | NULL；原始 URL，便于反查 |
| status | enum | ACTIVE / ARCHIVED |
| created_by | bigint FK | |
| created_at | datetime | |

#### 5.2.8 ProjectCaseRef（项目用例池）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| project_id | bigint FK | |
| case_id | bigint FK CaseSetCase | |
| pinned_version | int | 引用的版本号 |
| added_by / added_at | – | |

唯一：`(project_id, case_id)`。

#### 5.2.9 Round（测试轮次）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| project_id | bigint FK | |
| name | varchar(64) | 项目内唯一 |
| software_version | varchar(64) | NOT NULL |
| previous_round_id | bigint FK Round | NULL（按上轮筛选时引用） |
| filter_expr | json | 见 §5.3 |
| status | enum | DRAFT/IN_PROGRESS/PAUSED/CLOSED |
| planned_start / planned_end | datetime | |
| actual_start / actual_end | datetime | |
| created_by | bigint FK | |
| created_at | datetime | |

#### 5.2.10 RoundCaseInstance（轮次中一份执行实例）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| round_id | bigint FK | |
| case_id | bigint FK CaseSetCase | |
| case_version | int | 执行时锁定的版本 |
| assignee_user_id | bigint FK | |
| executor_user_id | bigint FK | NULL，实际执行人 |
| result | enum | PENDING/P/F/BLOCK/NP/NT |
| actual_result | text | result=F/BLOCK 必填 |
| comment | text | |
| executed_at | datetime | |
| duration_seconds | int | NULL |

索引：`(round_id, assignee_user_id, result)`。

#### 5.2.11 DefectLink

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | bigint PK | |
| round_case_instance_id | bigint FK | |
| tb_project_id | varchar(32) | |
| tb_task_id | varchar(32) | UNIQUE；Teambition task 唯一 ID |
| tb_bug_section_id | varchar(32) | |
| tb_url | varchar(500) | 跳转链接（`https://www.teambition.com/project/{pid}/task/{tid}`） |
| tb_status | varchar(32) | 缓存：未开始/进行中/已完成/已关闭… |
| tb_priority | int | 缓存 |
| tb_severity | varchar(32) | 缓存（自定义字段） |
| tb_executor_id | varchar(32) | 缓存 |
| occurrence_prob | enum | `MUST`/`HIGH`/`LOW`/`RARE`（必现/高概率/低概率/偶现） |
| last_synced_at | datetime | |
| created_by / created_at | – | |

#### 5.2.12 AuditLog

| 字段 | 类型 |
| --- | --- |
| id / user_id / action / target_type / target_id / before / after / ip / ua / created_at | – |

#### 5.2.13 DingtalkPushLog

| 字段 | 类型 |
| --- | --- |
| id / user_id / round_id / payload / status(SUCCESS/FAILED/RETRYING) / retry_count / last_error / created_at | – |

### 5.3 筛选表达式（FR-5 核心）

存为布尔树 JSON：

```json
{
  "op": "AND",
  "children": [
    { "field": "priority", "operator": "IN", "value": ["P0", "P1"] },
    {
      "op": "OR",
      "children": [
        { "field": "tags", "operator": "CONTAINS", "value": "smoke" },
        { "field": "previousResult", "operator": "IN", "value": ["F", "BLOCK"], "refRoundId": 1234 }
      ]
    }
  ]
}
```

支持字段：`priority` / `tags` / `module` / `type` / `previousResult` / `createdAt` / `caseSet`。  
后端编译为 SQL `WHERE` 子句（或 ES Query DSL，依存储选型）。

---

## 6. 接口与集成需求

> 约定：所有内部 API 走 RESTful，`/api/v1/...`；统一响应 `{code, message, data, traceId}`；认证 `Authorization: Bearer <jwt>`；时间 ISO 8601 UTC。错误码按模块前缀（FR-X 节内已列）。

### 6.1 内部 API 主要端点（示例，研发以 OpenAPI 为准）

#### 认证与用户

| 方法 | 路径 | 描述 |
| --- | --- | --- |
| POST | `/api/v1/auth/register` | 注册（含发码） |
| POST | `/api/v1/auth/verify-email` | 验证邮箱 |
| POST | `/api/v1/auth/login` | 登录 |
| POST | `/api/v1/auth/refresh` | 刷新 token |
| POST | `/api/v1/auth/logout` | 登出 |
| GET | `/api/v1/users/me` | 获取当前用户 |

#### 用例集 / 用例

| 方法 | 路径 |
| --- | --- |
| GET / POST | `/api/v1/case-sets` |
| GET / PUT / DELETE | `/api/v1/case-sets/{id}` |
| POST | `/api/v1/case-sets/{id}/import`（multipart） |
| GET | `/api/v1/case-sets/{id}/export` |
| GET / POST | `/api/v1/case-sets/{id}/cases` |
| GET / PUT / DELETE | `/api/v1/cases/{id}` |
| GET | `/api/v1/cases/{id}/versions` |

#### 项目

| 方法 | 路径 |
| --- | --- |
| GET / POST | `/api/v1/projects` |
| GET / PUT / DELETE | `/api/v1/projects/{id}` |
| GET / POST / DELETE | `/api/v1/projects/{id}/members` |
| GET / POST / DELETE | `/api/v1/projects/{id}/cases`（项目用例池） |

#### 轮次与执行

| 方法 | 路径 |
| --- | --- |
| GET / POST | `/api/v1/projects/{id}/rounds` |
| GET / PATCH | `/api/v1/rounds/{id}` |
| POST | `/api/v1/rounds/{id}/publish` |
| POST | `/api/v1/rounds/{id}/close` |
| POST | `/api/v1/rounds/{id}/assign`（批量分配） |
| GET | `/api/v1/rounds/{id}/cases`（执行实例列表，支持筛选） |
| PATCH | `/api/v1/round-cases/{instanceId}/result` |
| GET | `/api/v1/rounds/{id}/report` |
| GET | `/api/v1/rounds/{id}/report/export?format=pdf|excel` |

#### 缺陷

| 方法 | 路径 |
| --- | --- |
| POST | `/api/v1/round-cases/{instanceId}/defects`（提交并联动 TB） |
| GET | `/api/v1/projects/{id}/defects`（列表 + 看板数据） |
| GET | `/api/v1/projects/{id}/defects/dashboard` |
| POST | `/api/v1/defects/sync`（管理员手动触发同步） |

#### 系统管理

| 方法 | 路径 |
| --- | --- |
| GET / PUT | `/api/v1/admin/config/dingtalk` |
| GET / PUT | `/api/v1/admin/config/tb` |
| GET / PUT | `/api/v1/admin/config/email` |
| GET | `/api/v1/admin/audit-logs` |

### 6.2 关键请求/响应样例

**创建轮次**

```http
POST /api/v1/projects/123/rounds
Content-Type: application/json

{
  "name": "V2.3 回归 R1",
  "softwareVersion": "2.3.0-rc1",
  "previousRoundId": 1122,
  "plannedStart": "2026-06-10T01:00:00Z",
  "plannedEnd":   "2026-06-15T10:00:00Z",
  "filterExpr": { "op":"AND", "children":[ ... ] }
}
```

响应：

```json
{
  "code": 0,
  "data": { "id": 1234, "status": "DRAFT", "matchedCaseCount": 412 },
  "traceId": "..."
}
```

**提交执行结果**

```http
PATCH /api/v1/round-cases/55678/result

{
  "result": "F",
  "actualResult": "点击保存按钮无响应，控制台报 500",
  "attachmentIds": [9001, 9002]
}
```

### 6.3 钉钉集成（FR-6）

- API：钉钉企业内部应用 OAPI `topapi/message/corpconversation/asyncsend_v2`。
- 凭证：AppKey / AppSecret → 缓存 access_token（2h）+ 提前 5min 刷新。
- 消息类型：`oa` 模板，含跳转 `pcUrl`/`mobileUrl`。
- 映射：优先 `dingtalk_user_id`；缺失时按手机号经 `topapi/v2/user/getbymobile` 反查。
- 限流：钉钉 QPS 20/s，本系统侧令牌桶 10/s 调用。

### 6.4 SSO / LDAP（V1.5，OP-7）

- V1：仅邮箱密码。
- V1.5：支持 LDAP / 钉钉扫码 / OIDC，独立设计文档。

### 6.5 Teambition 缺陷平台（FR-9）

**平台**：阿里 Teambition（`https://www.teambition.com`）。

**示例配置**：
- 缺陷分组 URL：`https://www.teambition.com/project/60b75d1971a638c8771a9ab6/bug/section/635bbbc175417100120310a0`
- 解析为：`tb_project_id = 60b75d1971a638c8771a9ab6`，`tb_bug_section_id = 635bbbc175417100120310a0`

**鉴权**：使用 Teambition 开放平台 OAuth 2.0；SysAdmin 配置 AppId / AppSecret，颁发的 access_token 系统侧统一管理与刷新；后端调用 Teambition Open API 时附带 `X-Tenant-Id` / `Authorization: OAuth2 <token>`（以 Teambition 当前最新文档为准）。

**主要端点（按 Teambition Open API 推荐路径，最终以官方文档为准）**

| 用途 | 方法 / 路径（示例） |
| --- | --- |
| 创建缺陷（Task，taskType=bug） | `POST /api/projects/{projectId}/tasks` |
| 查询缺陷 | `GET /api/tasks/{taskId}` |
| 查询项目成员 | `GET /api/projects/{projectId}/members` |
| 上传附件 | `POST /api/projects/{projectId}/attachments` |
| 状态枚举（任务流） | `GET /api/projects/{projectId}/taskflowstatuses` |

**字段映射（本系统 → Teambition Task/Bug）**

| 本系统 | Teambition 字段 |
| --- | --- |
| 缺陷标题 | `content` |
| 复现步骤 + 预期 + 实际 | `note`（Markdown，结构化拼装） |
| 严重程度 | `priority`（映射：Blocker=3 / Critical=2 / Major=1 / Minor=0；或自定义字段 `severity`） |
| 关联用例编号 | 自定义字段 `caseCode`，并在 `note` 顶部追加链接 |
| 关联轮次 | 自定义字段 `roundName` + `note` 末尾追加 |
| 被测版本 | 自定义字段 `softwareVersion` |
| 经办人 | `executorId`（Teambition userId） |
| 缺陷分组（Section） | `tfsId` 或创建时直接落到 `bugSectionId` |
| 标签 | `tagIds`（可选） |
| 附件 | 单独 `POST /attachments`，拿到 `attachmentIds` 写入 `attachmentsCount` / `objectLinks` |

**用户映射**：以邮箱为锚 → 调用 Teambition 成员接口匹配，缓存映射；缺失时 PM 在项目设置中手动维护。

**状态回写**：每 5 分钟轮询 `GET /api/tasks/{taskId}`，更新本系统 `DefectLink.tb_status` 等缓存字段；状态枚举从任务流接口动态拉取，配置内置映射表（如 `未开始` / `进行中` / `已完成` / `已关闭`）。

**反向跳转**：本系统缺陷详情页跳转 `https://www.teambition.com/project/{tb_project_id}/task/{tb_task_id}`。

**限流与重试**：Teambition Open API 全局 QPS ≤ 20，本系统侧令牌桶 10/s；失败 3 次指数退避，终态失败入告警队列。

### 6.6 邮件服务

- SMTP 配置（host/port/user/pass/tls）；用途：注册验证码、密码重置、报告订阅（V1.5）。

### 6.7 文件存储

- V1：本地磁盘 + Nginx 静态托管；目录按 `/{yyyy}/{mm}/{dd}/{uuid}.ext`。
- V1.5+：对象存储（MinIO / OSS / S3），抽象 `StorageService` 接口。

---

## 7. UI / 页面结构与交互

### 7.1 信息架构（导航）

```
顶部导航：[用例集] [项目] [报告] [缺陷] [管理]   右侧：[消息] [用户]
项目内左侧二级菜单：
  - 概览
  - 用例池
  - 测试轮次
  - 执行（跳到当前用户的执行入口）
  - 缺陷
  - 报告
  - 成员与设置
```

### 7.2 关键页面（要点）

| 页面 | 区块要点 |
| --- | --- |
| 登录 / 注册 | 邮箱、密码、注册引导；错误提示明确 |
| 用例集列表 | 卡片/表格切换；按业务线筛选；批量归档 |
| 用例集详情 | 左：模块树；右：用例列表（多选、批量操作）；顶部：导入/导出/新增 |
| 用例编辑抽屉 | 富文本步骤表；可拖拽排序；标签自动补全 |
| 项目列表 | 我参与的 / 全部；最近活跃排序 |
| 项目用例池 | 与用例集详情类似；多了"来源用例集"列、"重新拉取最新版本"操作 |
| 轮次列表 | 进行中 / 已关闭 Tab；进度条 + 通过率快览 |
| 轮次创建向导 | 4 步：基本信息 → 策略筛选（可视化条件构造器）→ 分配 → 确认发布 |
| 执行页 | 详见 FR-7 双栏布局 |
| 缺陷提交弹窗 | 字段分为"自动回填（只读折叠）" + "用户填写"两区 |
| 报告页 | 概览卡片 + 图表 + 缺陷表；导出按钮 |
| 缺陷看板 | 6 卡片 + 4 图表；筛选条同步 URL |
| 管理后台 | 用户、角色、集成配置、审计日志 |

### 7.3 交互通用规范

- 所有列表支持：分页（默认 20）、列设置、URL 同步筛选条件。
- 危险操作（删除、关闭轮次）二次确认。
- 操作反馈：Toast 成功；阻塞错误 Modal；非阻塞警告 Alert。
- 国际化：V1 中文；预留 i18n（资源文件）。
- 响应式：≥ 1280px 桌面优先；移动端仅做"接收钉钉跳转 → 执行单条用例"的最小适配（V1.5）。

### 7.4 异常态

- 空数据：图示 + 主操作引导。
- 加载：骨架屏；> 1s 显 Loading。
- 错误：错误码 + 重试。
- 权限不足：403 友好页 + 申请权限入口。

---

## 8. 非功能需求

### 8.1 性能

| 指标 | 目标 |
| --- | --- |
| 用例集列表 / 用例详情 P95 | < 500ms |
| 执行结果保存 P95 | < 300ms |
| 报告生成（≤ 5000 用例） | < 5s 在线；< 30s 导出 PDF |
| 钉钉推送（单条） | < 2s |
| 并发在线用户 | 200 |
| 单项目用例池 | 50,000 |
| 单轮次执行实例 | 20,000 |

### 8.2 安全

- **认证**：JWT；refresh token 单设备/多设备策略可配置。
- **授权**：RBAC + 资源 ACL（项目 / 用例集级）；所有 API 走统一中间件鉴权。
- **传输**：全站 HTTPS（TLS 1.2+）。
- **密码**：bcrypt cost ≥ 12；不可逆；找回走邮箱链接（1h 失效）。
- **越权**：水平/垂直越权用例纳入安全测试；按 OWASP Top 10 自查。
- **注入**：参数化查询；富文本入库前 XSS 过滤；文件上传 MIME + 扩展名双校验 + 病毒扫描（V1.5）。
- **审计**：所有写操作记 `AuditLog`；保留 ≥ 1 年。
- **敏感数据脱敏**：日志中邮箱/手机号脱敏；TB token / 钉钉 secret 加密存储（KMS / 配置中心）。
- **速率限制**：登录 / 注册接口 IP 维度限流；钉钉推送批量聚合。
- **CSRF**：纯 API + Bearer Token 模式，免疫；如有同域 Cookie 场景启用 SameSite=strict。

### 8.3 可用性

- 月可用性 ≥ 99.5%。
- 缺陷 SLA 默认阈值（缺陷看板使用）：

| 严重 | 修复 SLA（工作日） |
| --- | --- |
| Blocker | 1 |
| Critical | 2 |
| Major | 5 |
| Minor | 10 |
| Trivial | 不限 |

### 8.4 可扩展性

- 无状态后端，水平扩展（K8s Deployment）。
- 长任务（导入、报告导出、钉钉推送、TB 同步）走异步队列（RabbitMQ / Redis Stream / Kafka）。
- 数据库读写分离预留；按 `project_id` 分库分表预留。

### 8.5 可观测性

- 日志：结构化 JSON，集中收集（ELK / Loki）。
- 监控：API QPS / 延迟 / 错误率 / 队列堆积（Prometheus + Grafana）。
- 告警：P95 超阈值、TB 同步失败率 > 5%、钉钉失败率 > 5% → 企业微信/钉钉告警。
- 链路追踪：OpenTelemetry，`traceId` 贯通响应体。

### 8.6 备份与恢复

- 数据库每日全备 + 30 天 binlog；保留 90 天。
- 文件存储每日增量备份。
- RTO ≤ 4h；RPO ≤ 24h。

### 8.7 合规

- 用户数据本地化存储；公司内网部署优先；不出域。
- 符合公司信息安全 / 数据分级管理规定。

---

## 9. 技术约束与依赖

> 以下为推荐技术栈，可由架构组最终决定。

| 层 | 推荐 |
| --- | --- |
| 前端 | Vue 3 + TypeScript + Pinia + Element Plus / Ant Design Vue；富文本：tiptap；表格：vxe-table |
| 后端 | Spring Boot 3（Java 17）或 NestJS（备选）；REST + OpenAPI；JWT |
| 数据库 | PostgreSQL 14（用 JSONB + GIN 索引利于 tags / filterExpr） |
| 缓存 / 限流 / 队列 | Redis 7；MQ 用 RabbitMQ 或直接 Redis Stream |
| 文件存储 | V1 本地；V1.5+ MinIO / OSS |
| 部署 | Docker + K8s；CI：GitLab CI / Jenkins |
| 监控 | Prometheus + Grafana + Loki；APM：SkyWalking 或 OpenTelemetry Collector |
| 浏览器支持 | Chrome / Edge 最新两个版本；不支持 IE |

外部依赖：
- 钉钉企业内部应用（AgentId / AppKey / AppSecret）。
- TB 缺陷管理平台 API（OP-5 待定）。
- 公司邮件 SMTP。
- 公司域名 + HTTPS 证书。

---

## 10. 验收标准

### 10.1 功能验收（每个 FR 必过）

- FR-1 ～ FR-11 主流程 + 主要异常流均有用例覆盖且通过。
- 权限矩阵每行每列均有用例覆盖，越权请求统一 403。
- 跨模块联动：执行 F → TB 建单 → 缺陷列表显示 → 报告统计 → 看板更新，端到端打通。

### 10.2 性能验收

- 8.1 性能指标在 200 并发下达标（JMeter / k6 压测报告）。

### 10.3 安全验收

- 通过公司安全团队漏扫（无高危）+ OWASP Top 10 自检。

### 10.4 数据验收

- 一份 5000 条 Excel 用例导入耗时 ≤ 60s 且 0 数据丢失。
- 用例修订后版本回放可还原任意历史。
- 轮次 CLOSED 后报告数据不再变化。

### 10.5 集成验收

- 钉钉推送成功率 ≥ 99%（24h 滑动）。
- TB 双向同步：创建 100% 成功；状态回写延迟 ≤ 10min；TB 不可达 1h 内恢复后自动补齐。

---

## 11. 里程碑与版本规划（建议）

| 版本 | 范围 | 周期（建议） |
| --- | --- | --- |
| V1.0 | FR-1 ～ FR-11 全部 | 主交付 |
| V1.1 | 报告比对、用例评审流程、移动端最小适配、报告订阅 | 增强 |
| V1.5 | SSO（LDAP/OIDC/钉钉扫码）、对象存储、自动化用例对接（CI Webhook） | 拓展 |
| V2.0 | 测试需求管理、用例 AI 生成 / 评审、缺陷智能聚类 | 演进 |

---

## 12. 附录

### A. 开放问题（评审时确认）

| 编号 | 问题 | 推荐方案 |
| --- | --- | --- |
| OP-1 | 用例字段模板 | 已对齐 `用例模版.xlsx`（见 §FR-3 与 §5.2.4） |
| OP-2 | 用例集修订是否自动同步到已引用项目？ | 项目侧手动"刷新版本"，避免回归途中数据漂移 |
| OP-3 | 是否启用用例评审流程（DRAFT→REVIEW→ACTIVE）？ | V1 默认关闭，由用例集配置开关 |
| OP-4 | 执行率分母是否扣除 NP / NT？ | 是（如 §3.3） |
| OP-5 | TB 平台 | 已确定为 **Teambition**，OAuth2；字段映射见 §6.5 |
| OP-6 | 轮次报告模板 | 已对齐 `轮次测试报告.xlsx`（见 §FR-10） |
| OP-6a | "截止本轮总体测试充分性"分子是否包含本轮？ | 已确定：**包含本轮**（累计口径） |
| OP-7 | 是否 V1 即提供 SSO？ | V1.5 提供 |
| OP-8 | 多人执行同一用例是否允许？ | 允许（多份 RoundCaseInstance），默认报告取最新（可配置） |
| OP-9 | 用例集可见范围 PUBLIC 是否需要租户隔离？ | V1 单租户，无需 |
| OP-10 | 富文本图片存储与外链策略 | 统一上传到文件存储，禁用外链以防失效 |
| OP-11 | 出现概率字段值域 | 推荐 `必现` / `高概率` / `低概率` / `偶现`（与报告 Sheet2 一致） |

### B. 术语表

| 术语 | 含义 |
| --- | --- |
| 用例集（CaseSet） | 用例的资产库，跨项目可复用 |
| 用例池（ProjectCasePool） | 项目从用例集挑选的用例集合 |
| 轮次（Round） | 项目下的一次完整测试执行 |
| 执行实例（RoundCaseInstance） | 一份用例在某轮次中分配给某人后的执行记录 |
| TB | 缺陷管理平台（External，待确认） |
| RBAC | 基于角色的访问控制 |
| SLA | 服务等级目标 |

### C. 风险

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| TB API 不稳定 / 协议变更 | 缺陷联动失败 | 抽象适配层，重试 + 告警 + 草稿暂存 |
| 钉钉令牌过期 / 限流 | 推送丢失 | 双 buffer 刷新 + 失败队列 + 邮件兜底 |
| 用例集修订与项目快照一致性 | 数据漂移 | 引用版本号锁定 + 显式刷新 |
| 并发执行同一实例 | 结果覆盖 | 后端版本号乐观锁 + 前端实时提示 |
| 大量导入导致 DB 抖动 | 性能 | 分批 + 异步 + 限流 |

### D. 与源需求的逐条映射

| 源功能点 | 本 PRD 覆盖 |
| --- | --- |
| 功能 1 注册 | FR-1 |
| 功能 2 登录 | FR-2 |
| 功能 3 用例集管理 | FR-3 |
| 功能 4 项目管理 | FR-4 |
| 功能 5 测试轮次 + 筛选策略 | FR-5（+ §5.3 表达式树） |
| 功能 6 钉钉推送 | FR-6（+ §6.3） |
| 功能 7 执行界面（F/P/Block/NP/NT） | FR-7（+ §3.3 状态机） |
| 功能 8 执行界面修订/新增/删除（回写用例集） | FR-8 |
| 功能 9 F → 提交缺陷联动 TB（回填步骤、版本） | FR-9（+ §6.5） |
| 功能 10 轮次测试报告 | FR-10 |
| 功能 11 项目缺陷管理 + 看板 | FR-11 |

— 文档结束 —
